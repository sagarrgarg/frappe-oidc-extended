{
 "actions": [],
 "autoname": "format:{provider}",
 "creation": "2023-06-16 19:03:33.874968",
 "default_view": "List",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "provider",
  "claims_section",
  "given_name_claim_name",
  "family_name_claim_name",
  "email_claim_name",
  "groups_claim_name",
  "column_break_0",
  "users_section",
  "user_provisioning",
  "new_user_type",
  "require_verified_email",
  "match_users_by_username",
  "verification_section",
  "verify_id_token_signature",
  "jwks_url",
  "issuer",
  "roles_section",
  "fallback_role_profiles",
  "group_role_mappings",
  "unmapped_user_action",
  "disable_unmapped_users",
  "modules_section",
  "fallback_module_profile",
  "group_module_mappings",
  "reconciliation_section",
  "enable_reconciliation",
  "reconciliation_frequency",
  "absent_user_action",
  "directory_type",
  "directory_url",
  "directory_client_id",
  "directory_client_secret",
  "directory_api_token",
  "webhook_secret",
  "last_reconciled_on"
 ],
 "fields": [
  {
   "fieldname": "provider",
   "fieldtype": "Link",
   "in_list_view": 1,
   "label": "Social Login (OIDC) Provider",
   "options": "Social Login Key",
   "reqd": 1,
   "set_only_once": 1,
   "unique": 1
  },
  {
   "fieldname": "claims_section",
   "fieldtype": "Section Break",
   "label": "Token Claims"
  },
  {
   "default": "given_name",
   "fieldname": "given_name_claim_name",
   "fieldtype": "Data",
   "label": "Given Name Claim"
  },
  {
   "default": "family_name",
   "fieldname": "family_name_claim_name",
   "fieldtype": "Data",
   "label": "Family Name Claim"
  },
  {
   "default": "email",
   "fieldname": "email_claim_name",
   "fieldtype": "Data",
   "label": "Email Claim"
  },
  {
   "default": "groups",
   "description": "The name of the claim that includes a list of user groups.",
   "fieldname": "groups_claim_name",
   "fieldtype": "Data",
   "label": "Groups Claim"
  },
  {
   "fieldname": "column_break_0",
   "fieldtype": "Column Break"
  },
  {
   "fieldname": "users_section",
   "fieldtype": "Section Break",
   "label": "Users"
  },
  {
   "default": "Use Social Login Key Setting",
   "description": "Whether a login by someone without a Frappe user account creates one. \"Use Social Login Key Setting\" follows the Sign-ups field of the Social Login Key, falling back to the site's website signup setting, which is what Frappe's own social logins do. \"Always Create Users\" provisions everyone the identity provider lets through, which is only safe when the provider restricts who may reach this callback.",
   "fieldname": "user_provisioning",
   "fieldtype": "Select",
   "label": "User Provisioning",
   "options": "Use Social Login Key Setting\nAlways Create Users\nNever Create Users"
  },
  {
   "default": "Website User",
   "description": "The User Type given to users this app creates. Frappe overrides the two standard types on every save - a user holding any role with desk access becomes a System User, everyone else a Website User (User.set_system_user) - so on a site where System Users are billed seats, the role profiles you map decide the cost. A custom User Type is honoured as set.",
   "fieldname": "new_user_type",
   "fieldtype": "Link",
   "label": "User Type For New Users",
   "options": "User Type"
  },
  {
   "default": "1",
   "description": "Refuses a login when the identity provider says the email address has not been verified. Users are matched to Frappe accounts by email address, so an unverified one lets someone who registered another person's address take over their account. Providers that do not send the claim are unaffected.",
   "fieldname": "require_verified_email",
   "fieldtype": "Check",
   "label": "Require A Verified Email Address"
  },
  {
   "default": "0",
   "description": "Also matches a login to a Frappe user whose username equals the identity provider's user id claim, when nothing else matched. Only needed for users provisioned by versions of this app that stored the claim there: the claim is not a Frappe username, so this can match an unrelated account that happens to carry that name.",
   "fieldname": "match_users_by_username",
   "fieldtype": "Check",
   "label": "Match Users By Username"
  },
  {
   "fieldname": "verification_section",
   "fieldtype": "Section Break",
   "label": "ID Token Verification"
  },
  {
   "default": "1",
   "description": "Verifies the signature of the id token against the signing keys of the identity provider. Turn this off only to debug a provider whose keys cannot be reached: an unverified id token is a bearer of unauthenticated claims, including the groups that decide the user's roles.",
   "fieldname": "verify_id_token_signature",
   "fieldtype": "Check",
   "label": "Verify ID Token Signature"
  },
  {
   "depends_on": "eval:doc.verify_id_token_signature",
   "description": "The JWKS endpoint of the identity provider, for example https://auth.example.com/application/o/erpnext/jwks/. Leave empty to read it from the provider's OpenID discovery document.",
   "fieldname": "jwks_url",
   "fieldtype": "Data",
   "label": "JWKS URL"
  },
  {
   "depends_on": "eval:doc.verify_id_token_signature",
   "description": "The expected \"iss\" claim of the id token. When set, tokens issued by a different issuer are rejected, and the discovery document is read from this URL. Defaults to the base URL of the Social Login Key.",
   "fieldname": "issuer",
   "fieldtype": "Data",
   "label": "Issuer"
  },
  {
   "fieldname": "roles_section",
   "fieldtype": "Section Break",
   "label": "Roles"
  },
  {
   "fieldname": "fallback_role_profiles",
   "fieldtype": "Table",
   "label": "Fallback Role Profiles",
   "options": "OIDC Fallback Role Profile",
   "description": "If a user has no matching group mappings, they will be assigned these fallback role profiles."
  },
  {
   "fieldname": "group_role_mappings",
   "fieldtype": "Table",
   "label": "Group To Role Mappings",
   "options": "OIDC Group To Role Mapping",
   "description": "Map OIDC groups to ERPNext roles. This assigns permissions allowing users to access data and make changes based on their role."
  },
  {
   "default": "Keep Existing Roles",
   "description": "What to do when the identity provider returns no group that matches a mapping above and no fallback role profile is configured. \"Keep Existing Roles\" leaves the user's current roles untouched, \"Remove All Roles\" strips them so that removing a user from every group in the identity provider de-provisions them, and \"Deny Login\" refuses the login instead.",
   "fieldname": "unmapped_user_action",
   "fieldtype": "Select",
   "label": "When No Group Matches",
   "options": "Keep Existing Roles\nRemove All Roles\nDeny Login"
  },
  {
   "default": "0",
   "description": "Disables the Frappe account of a user whose groups match no mapping above or below, and grant no fallback profile. Refusing the login is not the same as closing the account: a user denied at the door keeps an enabled account, and with it its API keys, its seat and its local password, none of which the identity provider can revoke. This composes with the setting above rather than replacing it - that one still decides what happens to the roles - so leaving it on \"Keep Existing Roles\" and turning this on lets group membership gate access without this app ever touching anybody's roles. A user whose group comes back is enabled again at their next login, so a membership removed by mistake is not permanent; by the same token, an account disabled by hand here is reopened if the provider still vouches for it. Administrator, Guest and the last enabled System Manager are never disabled - the guard is logged when it fires. Off by default.",
   "fieldname": "disable_unmapped_users",
   "fieldtype": "Check",
   "label": "Disable Users With No Mapped Group"
  },
  {
   "fieldname": "modules_section",
   "fieldtype": "Section Break",
   "label": "Modules Mapping"
  },
  {
   "description": "If a user has no matching group mappings, they will be assigned this fallback module profile.",
   "fieldname": "fallback_module_profile",
   "fieldtype": "Link",
   "label": "Fallback Module Profile",
   "options": "Module Profile"
  },
  {
   "fieldname": "group_module_mappings",
   "fieldtype": "Table",
   "label": "Group To Module Mappings",
   "options": "OIDC Group To Module Mapping",
   "description": "Map OIDC groups to ERPNext modules. This controls which modules are visible on the workspace/sidebar for the user."
  },
  {
   "fieldname": "reconciliation_section",
   "fieldtype": "Section Break",
   "label": "Reconciliation"
  },
  {
   "fieldname": "enable_reconciliation",
   "fieldtype": "Check",
   "default": "0",
   "label": "Enable Reconciliation",
   "description": "Asks the identity provider on a schedule which users still exist, are still enabled and are still in which groups, and applies the answer. Nothing in OpenID Connect pushes this: removing someone from a group, or from the directory, otherwise reaches Frappe only at their next login - which, once they are gone, never comes."
  },
  {
   "fieldname": "reconciliation_frequency",
   "fieldtype": "Select",
   "default": "Daily",
   "depends_on": "eval:doc.enable_reconciliation",
   "label": "Frequency",
   "options": "Daily\nHourly"
  },
  {
   "fieldname": "absent_user_action",
   "fieldtype": "Select",
   "default": "Report Only",
   "depends_on": "eval:doc.enable_reconciliation",
   "label": "When A User Is Gone Or Disabled",
   "description": "What to do with a Frappe user the directory no longer has, or has disabled. \"Report Only\" writes to the log and changes nothing. The other two strip the role profiles and module profile first, so that a user re-enabled locally does not return with the entitlements they left with.",
   "options": "Report Only\nRemove All Roles\nDisable User"
  },
  {
   "fieldname": "directory_type",
   "fieldtype": "Select",
   "depends_on": "eval:doc.enable_reconciliation",
   "label": "Directory Type",
   "options": "\nKeycloak\nAuthentik"
  },
  {
   "fieldname": "directory_url",
   "fieldtype": "Data",
   "depends_on": "eval:doc.enable_reconciliation",
   "label": "Directory URL",
   "description": "For Keycloak, the realm URL - the same base URL the Social Login Key uses, for example https://idp.example.com/realms/erp. For authentik, its base URL."
  },
  {
   "fieldname": "directory_client_id",
   "fieldtype": "Data",
   "depends_on": "eval:doc.directory_type == 'Keycloak'",
   "label": "Service Account Client ID",
   "description": "A Keycloak client with client authentication and service account roles on, whose service account holds view-users from realm-management."
  },
  {
   "fieldname": "directory_client_secret",
   "fieldtype": "Password",
   "depends_on": "eval:doc.directory_type == 'Keycloak'",
   "label": "Service Account Client Secret"
  },
  {
   "fieldname": "directory_api_token",
   "fieldtype": "Password",
   "depends_on": "eval:doc.directory_type == 'Authentik'",
   "label": "API Token",
   "description": "authentik matches users by email address: what it puts in `sub` depends on the provider's subject mode."
  },
  {
   "depends_on": "eval:doc.enable_reconciliation",
   "description": "Shared secret for the webhook endpoint at /api/method/oidc_extended.reconciliation.webhook/&lt;provider&gt;. Keycloak has no webhook of its own; an event listener provider deployed into it can call this whenever a user or a group membership changes, and the user is then re-checked against the directory at once instead of waiting for the next scheduled run. The call must present the secret as a bearer token or sign the body with it (HMAC-SHA256). Without a secret the endpoint is closed.",
   "fieldname": "webhook_secret",
   "fieldtype": "Password",
   "label": "Webhook Secret"
  },
  {
   "fieldname": "last_reconciled_on",
   "fieldtype": "Datetime",
   "label": "Last Reconciled On",
   "read_only": 1
  }
 ],
 "links": [],
 "modified": "2026-08-24 12:00:00.000000",
 "modified_by": "Administrator",
 "module": "OIDC Extended",
 "name": "OIDC Extended Configuration",
 "naming_rule": "Expression",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "title_field": "provider",
 "track_changes": 1
}
